<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.1.3/dist/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
   
    <title>Document</title>
</head>
<body>
    
    <div class="container mt-4">
        <h2>Community Workouts</h2>
    
        <div class="row">
            <% response.forEach(workout => { %>
                <% if(workout.visibilty){%>
                <div class="col-md-4 mb-4">
                    <div class="card">
                        <!-- Display workout image (assuming it's stored as Base64 data) -->
                        <img src="<%= workout.imageData ? 'data:image/jpeg;base64,' + workout.imageData.toString('base64') : 'rsz_logo-removebg-preview.png' %>" class="card-img-top" alt="<%= workout.workout_name %>">
                        <div class="card-body">
                            <h5 class="card-title"><%= workout.workout_name %></h5>
                            
                            <!-- Add more workout details as needed -->
                            <p class="card-text"><%= workout.workout_desc %></p>
                            <p class="card-text">Visibility: <%= workout.visibilty ? 'Public' : 'Private' %></p>
                            
                            <!-- Example: Display client details -->
                            <p class="card-text">Client: <%= workout.client_id ? workout.client_id : 'Unknown' %></p>
    
                            <!-- Add more workout details as needed -->
    
                            <!-- Example: Display a button or link to view more details -->
                            <a href="/workout/<%= workout.workout_id %>" class="btn btn-primary">View Details</a>
                        </div>
                    </div>
                </div>
            <% }}); %>
        </div>
    </div>
</body>
</html>